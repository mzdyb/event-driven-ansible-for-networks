---
- name: Enforce configuration block
  hosts: net01
  gather_facts: false

  roles:
    - ../roles/set_sot_variables
    - ../roles/apply_configuration_block_from_sot

  # tasks:
  #   - debug:
  #       msg:
  #         - "{{ config_template }}"
  #         - "{{ device }}"
